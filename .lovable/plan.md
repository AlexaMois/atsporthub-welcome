

# Защищённый вход для Генерального директора

## Новые файлы

### 1. `src/pages/PasswordPage.tsx`
- Маршрут: `/login/director`
- Центрированная форма (`max-w-sm mx-auto mt-16`)
- Кнопка «Назад» вверху слева — возврат на `/`
- Иконка Lock в синем круге (bg `#0099ff`, белая иконка, `w-16 h-16 rounded-full`)
- Заголовок «Защищённый вход», подтекст «Роль: Генеральный директор»
- Input `type="password"`, placeholder «Введите пароль»
- Кнопка «Войти» (bg `#0099ff`, white text, full width)
- Проверка пароля: `"atc2026"` — при совпадении `navigate('/dashboard/director')`, при ошибке — красный текст «Неверный пароль, попробуйте ещё раз»

### 2. `src/pages/DirectorDashboard.tsx`
- Маршрут: `/dashboard/director`
- Заглушка с текстом «Панель управления»
- Шапка как на главной (АТС Портал)

## Изменения в существующих файлах

### 3. `src/pages/Index.tsx`
- Клик по карточке «Генеральный директор» → `navigate('/login/director')` вместо `/role/...`

### 4. `src/App.tsx`
- Добавить маршруты `/login/director` и `/dashboard/director`

## Технические детали
- Используются существующие компоненты `Input` и `Button` из `src/components/ui/`
- Пароль проверяется на клиенте (статическая проверка, без Supabase)
- Состояние ошибки через `useState`

